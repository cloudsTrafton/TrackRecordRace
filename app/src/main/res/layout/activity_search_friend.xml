<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context="com.seniorproject.trafton.trackrecordrace.SearchFriendActivity">

    <TextView
        android:id="@+id/search_label"
        android:text="Search for a user by their username.\n Searches are case sensitive!"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
    <EditText
        android:id="@+id/friend_search"
        android:layout_width="match_parent"
        android:layout_below="@id/search_label"
        android:layout_height="wrap_content"/>
    <Button
        android:id="@+id/execute_search_button"
        android:text="Add Friend"
        android:backgroundTint="@color/colorPrimary"
        android:textColor="#ffffff"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/friend_search"/>

</RelativeLayout>
